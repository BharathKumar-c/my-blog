import Contact from '@/components/Contact';

export const metadata = {
  title: 'Contact US',
  discription: 'Make a enquiry for contact',
};

const ContactPage = () => {
  return <Contact />;
};

export default ContactPage;
